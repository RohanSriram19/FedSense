# Railway-optimized Dockerfile for FedSense Backend
FROM python:3.11-slim

# Set working directory
WORKDIR /app

# Install system dependencies required for ML libraries
RUN apt-get update && apt-get install -y \
    gcc \
    g++ \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Copy project files
COPY pyproject.toml README.md ./
COPY fedsense/ ./fedsense/

# Install Python dependencies - simplified versions to avoid conflicts
RUN pip install --no-cache-dir --upgrade pip setuptools wheel

# Install the package and core dependencies (avoiding version conflicts)
RUN pip install --no-cache-dir -e . && \
    pip install --no-cache-dir \
    uvicorn[standard] \
    gunicorn \
    jax[cpu] \
    flax \
    flwr \
    fastapi \
    pandas \
    numpy \
    scikit-learn \
    matplotlib \
    seaborn \
    pydantic \
    httpx

# Create necessary directories
RUN mkdir -p models data logs

# Expose port (Railway will set PORT env var)
EXPOSE $PORT

# Start command - Railway sets PORT automatically
CMD ["sh", "-c", "python -m fedsense.serve_fastapi --host 0.0.0.0 --port ${PORT:-8000}"]
